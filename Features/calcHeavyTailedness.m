%% Função que calcula a medida de Heavy-Tailedness 
%Entrada:
% mat         - região de uma imagem (patch)
%Saída:
% tailedness  - valor de Peakedness
%%
function tailedness = calcHeavyTailedness(mat)
    
    num_elem = size(mat,1)*size(mat,2);
        
end